{
    "type": "secret",
    "action": "create",
    "name": "${project}-${case:lower:envType}-${case:lower:envName}-vault",
    "stringData": {
        "PETRA_MY_VAULT_ROLE_ID": "${VAULT_ROLE_ID}",
        "PETRA_MY_VAULT_SECRET_ID": "${VAULT_SECRET_ID}"
    }
}