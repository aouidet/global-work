{
    "type": "secret",
    "action": "create",
    "name": "${project}-${case:lower:envType}-${case:lower:envName}-sg-monitoring",
    "stringData": {
        "APM_SECRET_TOKEN": "${apm.secret-token}",
        "sg-monitoring-elastic.user": "${sg-monitoring.elastic.user}",
        "sg-monitoring.elastic.password": "${sg-monitoring.elastic.password}"
    }
}